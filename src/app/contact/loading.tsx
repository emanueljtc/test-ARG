import { ContainerCenter, Loader } from '@/components';
import React from 'react';

export default function Loading() {
  return (
    <ContainerCenter>
      <Loader />
    </ContainerCenter>
  );
}

